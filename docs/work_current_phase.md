# Current phase: Local telemetry platform and application foundation

**Last reviewed:** 2026-09-20

| ID | Task | Status | Scope and approval dependency | Evidence |
| --- | --- | --- | --- | --- |
| DOC-01 | Review Python handoff and establish Kotlin migration plans | Completed | Documented target architecture, ordered implementation, dependencies, and acceptance gates. No external service or live database access needed. | Created `docs/PLAN.md`, `docs/architecture.md`, `docs/work_current_phase.md`, `docs/next_phase.md`, and `docs/complete_phases.md`. Inspected handoff, POM, README, source/config, and Docker SQL. `rtk proxy git ... status --short` listed only these five untracked documents; `rtk proxy git ... diff --check` exited 0 (does not inspect untracked files). No build, tests, live services, or formatting command run because this task changed Markdown only. Commit: none. |
| DOC-02 | Verify access to Python source repository | Completed | Read-only GitHub connectivity check for `EldadDor/RAG-dev-plane`; no clone, fetch, or repository change. | `rtk proxy git ls-remote https://github.com/EldadDor/RAG-dev-plane.git HEAD` exited 0 and returned `92a594e8e3bec694b1a893563f63d8a220605dcd`. No clone, fetch, build, tests, or live service run. Commit: none. |
| DOC-03 | Reconcile Kotlin plan with Python source | Completed | Inspected `EldadDor/RAG-dev-plane` at `92a594e8e3bec694b1a893563f63d8a220605dcd`, removed excluded scope, and revised the Kotlin plans for approval. | Reviewed Python configuration, API schemas/routers, identity/dependencies, pgvector store, services, migrations, tests, evaluation data, and `.env.example`; verified the Kotlin workspace has no `.env`. Updated `docs/PLAN.md`, `docs/architecture.md`, and `docs/next_phase.md`. `git diff --check` found only Markdown line-break whitespace, then corrected. No build, tests, database connection, provider call, or configuration-value transfer run. Commit: none. |
| DOC-04 | Expand parity breakdown and local observability plan | Completed | Replaced opaque task IDs, made all Python loaders/chunkers/assets early explicit work, and specified local OpenTelemetry, Prometheus, Grafana, and collector coverage independent of Langfuse. | Reviewed Python loader registry/implementations and observability modules. Rewrote `docs/PLAN.md`; updated `docs/next_phase.md` and `docs/architecture.md`. No build, tests, database connection, provider call, Docker service, or `.env` value transfer run. Commit: none. |
| DOC-05 | Refine parity evidence and Spring configuration plan | Completed | Prioritized API contract/golden evaluation evidence and translated Python configuration intent into standard Spring Boot YAML/profiles. | Verified `evaluation/golden-cases.jsonl` and `evaluation/golden-cases-heb.jsonl` are present. Updated `docs/PLAN.md`, `docs/next_phase.md`, and `docs/architecture.md`. No build, tests, database connection, provider call, Docker service, or configuration-value transfer run. Commit: none. |
| RDP-01 | Freeze API and evaluation evidence | Completed | Established Kotlin API contract fixtures and golden-case evaluation coverage from RAG-dev-plane; selected high-risk Python fixtures instead of porting every Python test. | Created `docs/api_evaluation_contract.md`. Verified 19 English and 13 Hebrew golden cases. Identified the active Python API modules and documented the source no-context-answer fixture discrepancy. `mvn spotless:apply` was not run: Maven first targeted inaccessible `C:\.m2`; RTK then misparsed the project-local repository argument. No build, tests, database connection, provider call, Docker service, or configuration-value transfer run. Commit: `fd466cd`. |
| RDP-02 | Build and Spring Boot configuration alignment | Completed | Aligned standard Spring Boot YAML/profile configuration with the Python setting names and defaults, removed shared-database persistence auto-configuration, and added typed application settings. No database schema change. | Added `AppProperties` plus a binding test; updated local/work profiles, provider dependencies, and Actuator exposure. `spring-ai-vector-store` provides only API types for inactive legacy POC source; no pgvector store starter, Flyway, Liquibase, or SQL initialization is present. Legacy POC vector and provider beans require the `legacy-poc` profile. `mvn -Dmaven.repo.local=E:\Workspace\AI_Stuff\RAG_Embabel-AI\.m2 spotless:apply test` passed: 1 configuration test passed; the 2 pre-existing POC tests remained skipped. No application startup, database connection, provider call, Docker service, or schema operation run. Commit: `ce8a16d`. |
| RDP-03 | Local telemetry platform and application foundation | In progress | Add an OpenTelemetry Collector, Prometheus, Grafana, Actuator metrics, and application telemetry configuration that operates locally with Langfuse disabled. No database schema change. | Added Actuator, Prometheus, OTLP metrics, and OpenTelemetry tracing configuration; common low-cardinality application/environment tags; and a Compose stack with Collector, Prometheus, Tempo, Grafana, and provisioned data sources. `mvn -Dmaven.repo.local=E:\Workspace\AI_Stuff\RAG_Embabel-AI\.m2 spotless:apply test` passed: 3 tests, 0 failures, 0 errors, 2 existing POC tests skipped. Docker Compose validation was not run because `docker` is not installed in this workspace. No application startup, database connection, provider call, container, or schema operation run. Checkpoint commit: `33c1401`. |
| RDP-02A | Apply confirmed local runtime topology | Completed | Set the local configuration for the laptop PostgreSQL/embedding endpoint and desktop chat endpoint; validate only the database connection during service startup. No schema validation, DDL, data write, model request, or container operation. | Updated the ignored workspace `.env` and `application-local.yml` for PostgreSQL/embeddings at `10.100.102.12` and desktop Ollama chat at `localhost:11434/v1`. Added an `ApplicationRunner` that calls JDBC `Connection.isValid(5)` and performs no SQL. `mvn -Dmaven.repo.local=E:\Workspace\AI_Stuff\RAG_Embabel-AI\.m2 spotless:apply test` passed: 4 tests, 0 failures, 0 errors, 2 existing POC tests skipped. Read-only `psql` inspection against the laptop verified schema versions `001_baseline` through `005_model_profiles`, 13 `rag` tables, and 92 columns including `vector`, JSONB, asset, session, cache, and model-profile fields. No application startup, model request, DDL, data write, or container operation run. Commit: `8f9714d`. |
| RDP-02B | Apply supplied bge-m3 local model configuration | Completed | Aligned standard Spring YAML and the ignored local `.env` with the supplied PostgreSQL target, BGE-M3 embedding profile, and desktop OpenAI-compatible chat model. No schema operation or model request. | Configured `postgres`, `rag.document_chunks_bge_m3`, 1024 dimensions, model profile `bge-m3`, embedding model `bge-m3:latest`, and chat model `dictalm2.0-instruct` at `127.0.0.1:1234/v1`. Added typed properties and binding assertions. `mvn -Dmaven.repo.local=E:\Workspace\AI_Stuff\RAG_Embabel-AI\.m2 spotless:apply test` passed: 4 tests, 0 failures, 0 errors, 2 existing POC tests skipped. No application startup, model request, database query, DDL, data write, or container operation run. Commit: `2b7e6e4`. |
| RDP-02C | Isolate obsolete vector-store POC components | Completed | Prevented the inactive legacy POC vector-store classes from participating in ordinary `local` or `local,legacy-poc` application startup. No vector-store bean, schema operation, model request, or database write. | POC vector components and ingestion controller now require `legacy-poc & vector-store-poc`; stale local Ollama POC configuration requires a further `legacy-ollama-chat` opt-in; work POC configuration requires `legacy-poc & work`. Selected OpenAI-compatible chat and Ollama embeddings explicitly in the local profile. `mvn -Dmaven.repo.local=E:\Workspace\AI_Stuff\RAG_Embabel-AI\.m2 spotless:apply test` passed: 4 tests, 0 failures, 0 errors, 2 existing POC tests skipped. A non-web startup with `local,legacy-poc` succeeded and validated the laptop PostgreSQL connection. No model request or schema operation ran. OTLP export correctly did not fail startup while the collector was absent. Commit: `3f90096`. |

The implementation backlog is in [next_phase.md](next_phase.md). The detailed sequence and gates are in [PLAN.md](PLAN.md).
